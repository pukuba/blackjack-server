type Query{
    test: String!
    login(id:String!, pw:String!): User
}

type User{
    code: Int!
    id: String!
    gold: Int!
    token: String
}

type Mutation{
    register(id:String!, pw:String!): User
    logout: User
}